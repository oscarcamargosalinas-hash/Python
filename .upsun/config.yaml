# Upsun configuration, generated using AI at: 2026-04-01T16:24:21-06:00
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: python:3.13

    hooks:
      build: |
        set -ex
        pip install -r requirements.txt
        # Export DATABASE_URL for Django or other frameworks that use it
        echo >> .environment 'export DATABASE_URL="$DB_SCHEME://$DB_USERNAME:$DB_PASSWORD@$DB_HOST:$DB_PORT/$DB_PATH"'

    web:
      commands:
        start: gunicorn learning_log.wsgi:application --bind 0.0.0.0:$PORT

    relationships:
      database: {}

services:
  database:
    type: postgresql:18

routes:
  https://{default}/:
    type: upstream
    upstream: app:http